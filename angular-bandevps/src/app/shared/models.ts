export interface UserData {
  created: number;
  creationDate: string;
  registered: boolean;
  email: string;
  fullName: string;
  id: string;
}

export interface UserInformation {
  address: string;
  occupation: string;
  phoneNumber: string;
}
